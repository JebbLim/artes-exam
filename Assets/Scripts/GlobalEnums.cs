using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalEnums : MonoBehaviour
{
    public enum GemType { Blue, Green, Red, Yellow, Purple, Length };
    public enum GameState { Wait, Move }
}
