using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GlobalEnums : MonoBehaviour
{
    public enum GemType { Blue, Green, Red, Yellow, Purple, Bomb, Length };
    public enum GameState { Wait, Move }
}
